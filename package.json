{
  "name": "invoice-api",
  "version": "1.0.0",
  "description": "Backend API for the invoicing app (customers + invoices)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
